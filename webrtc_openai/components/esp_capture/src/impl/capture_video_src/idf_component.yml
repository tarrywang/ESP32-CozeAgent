dependencies:
  espressif/esp_video:
    version: "~0.8.0"
    rules:
      - if: "target in [esp32p4]"

  espressif/esp32-camera:
    version: "~2.0.15"
    rules:
      - if: "target in [esp32s3,esp32s2,esp32]"
  esp_capture:
    path: ../../../../esp_capture
